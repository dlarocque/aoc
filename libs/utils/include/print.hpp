#pragma once

#include <string_view>
#include <vector>

using namespace std;

using ul = unsigned long;

namespace aoc::utils {
string to_string(const vector<long long>& v);
}  // namespace aoc::utils